# Node backend URL (deployed separately, e.g. Render/Railway/VPS).
# Copy this file to .env.local and fill the URL of your running backend.
# Example: https://your-backend.onrender.com
# Leave empty to run the site in pure localStorage mode.
VITE_API_URL=